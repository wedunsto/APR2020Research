--- example.CloseToZero
+++ example.CloseToZero
@@ -20,7 +20,8 @@
    */
   public int close_to_zero(int n) {
     if (n == 0) {
-      n++; // bug here
+      n--;
+	n++; // bug here
     } else if (n > 0) {
       n--;
     } else {
